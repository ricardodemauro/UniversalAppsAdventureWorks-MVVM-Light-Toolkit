<Page
    x:Class="AdventureWorksCatalog.View.ProductPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AdventureWorksCatalog.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="{Binding ProductPage, Source={StaticResource Locator}}">

    <Page.BottomAppBar>
        <CommandBar IsSticky="False">
            <AppBarButton Icon="ReShare" Label="Share" Command="{Binding ShareProductCommand}" />
            <AppBarButton Icon="Like" Label="Like" />
        </CommandBar>
    </Page.BottomAppBar>
    <Grid x:Name="LayoutRoot">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>



        <!-- Title Panel -->
        <StackPanel Grid.Row="0" Margin="19,0,0,0">
            <TextBlock Text="{Binding Company.Name}" Style="{ThemeResource TitleTextBlockStyle}" Margin="0,12,0,0"/>
            <TextBlock Text="{Binding Category.Name}" Margin="0,-6.5,0,26.5" Style="{ThemeResource HeaderTextBlockStyle}" CharacterSpacing="{ThemeResource PivotHeaderItemCharacterSpacing}"/>
        </StackPanel>

        <FlipView Grid.Row="1"
                ItemsSource="{Binding Category.Products}"
                SelectedItem="{Binding Product, Mode=TwoWay}">
            <FlipView.ItemTemplate>
                <DataTemplate>
                    <Grid Grid.Row="1" x:Name="ContentRoot" Margin="19,0,19,0">
                        <StackPanel VerticalAlignment="Top" HorizontalAlignment="Left">
                            <TextBlock Text="{Binding Name}" Margin="0,0,0,15" FontFamily="Segoe UI" FontSize="16"/>
                            <Border Background="#003D3D3D" Margin="0,0,0,15">
                                <Image Source="{Binding PhotoPath, Converter={StaticResource StringToImageConverter}}" Stretch="UniformToFill" AutomationProperties.Name="{Binding Name}"/>
                            </Border>
                            <TextBlock Text="{Binding Price, ConverterParameter=$ \{0:0.00\}, Converter={StaticResource StringFormatConverter}}"/>
                            <TextBlock Text="{Binding Description}" FontFamily="Segoe UI" HorizontalAlignment="Stretch" TextWrapping="WrapWholeWords"/>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </FlipView.ItemTemplate>
        </FlipView>
    </Grid>
</Page>

